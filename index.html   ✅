<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Schusszähler</title>

<style>
/* absolute Pflicht */
*,
*::before,
*::after {
    box-sizing: border-box;
}

body {
    margin: 0;
    padding: 16px;
    background: #cfc7bd;
    font-family: Calibri, Arial, sans-serif;
}

.container {
    max-width: 480px;
    margin: auto;
    background: #efe7dc;
    padding: 18px;
    border-radius: 14px;
    box-shadow: 0 6px 16px rgba(0,0,0,0.18);
}

/* Überschrift */
h1 {
    text-align: center;
    margin: 0 0 20px 0;
}

/* Labels */
label {
    display: block;
    margin-top: 14px;
    font-weight: bold;
    font-size: 14px;
}

/* === WICHTIGER FIX === */
input,
select,
button {
    width: 100%;
    max-width: 100%;
    display: block;
    padding: 12px;
    font-size: 16px;
    border-radius: 8px;
}

/* Inputs */
input,
select {
    margin-top: 6px;
    border: 1px solid #b7aa9a;
    background: #fff;
}

/* oberstes Feld leicht grün */
#geraetNeu {
    background: #eef7e9;
    border-color: #86b686;
}

/* 🔴 Spinner komplett entfernen → Problem gelöst */
input[type=number] {
    -moz-appearance: textfield;
}
input[type=number]::-webkit-outer-spin-button,
input[type=number]::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
}

/* Buttons */
button {
    border: none;
    cursor: pointer;
    margin-top: 14px;
}

.primary {
    background: #2f6fb0;
    color: #fff;
}

.secondary {
    background: #e6ded3;
    border: 1px solid #c1b4a4;
}

.danger {
    background: #b73a2d;
    color: #fff;
}

/* Gesamtbox */
.total {
    margin: 20px 0;
    padding: 18px;
    background: #fff;
    border-radius: 12px;
    text-align: center;
    font-size: 22px;
    font-weight: bold;
    border: 1px solid #c1b4a4;
}

.info {
    text-align: center;
    font-size: 12px;
    margin-top: 14px;
    color: #555;
}
</style>
</head>

<body>
<div class="container">

<h1>Schusszähler</h1>

<label>Neues Sportgerät</label>
<input type="text" id="geraetNeu" placeholder="z.B. Stgw 57">
<button class="secondary" onclick="neuesGeraet()">Hinzufügen</button>

<label>Sportgerät auswählen</label>
<select id="geraetSelect" onchange="ladeGeraet()">
    <option value="">– auswählen –</option>
</select>

<label>Startsaldo</label>
<input type="number" id="startsaldo" oninput="aktualisieren()">

<label>Neuer Eintrag</label>
<input type="number" id="neuerEintrag">
<button class="primary" onclick="addEintrag()">Eintrag hinzufügen</button>

<div class="total">
Gesamt<br>
<span id="total">0</span>
</div>

<button class="secondary" onclick="laufwechsel()">Neuer Lauf</button>
<button class="secondary" onclick="exportCSV()">Export CSV</button>
<button class="danger" onclick="geraetLoeschen()">Sportgerät löschen</button>

<div class="info">
Offlinefähig · Daten nur lokal gespeichert
</div>

</div>

<script>
let daten = JSON.parse(localStorage.getItem("schusszaehler")) || {};
let aktuellesGeraet = "";

function speichern() {
    localStorage.setItem("schusszaehler", JSON.stringify(daten));
}

function ladeDropdown() {
    const sel = document.getElementById("geraetSelect");
    sel.innerHTML = '<option value="">– auswählen –</option>';
    for (let g in daten) {
        const o = document.createElement("option");
        o.value = g;
        o.text = g;
        sel.appendChild(o);
    }
}

function neuesGeraet() {
    const name = geraetNeu.value.trim();
    if (!name) return;
    daten[name] = { startsaldo: 0, eintraege: [] };
    speichern();
    geraetNeu.value = "";
    ladeDropdown();
    geraetSelect.value = name;
    ladeGeraet();
}

function ladeGeraet() {
    aktuellesGeraet = geraetSelect.value;
    if (!aktuellesGeraet) return;
    startsaldo.value = daten[aktuellesGeraet].startsaldo;
    anzeigen();
}

function aktualisieren() {
    if (!aktuellesGeraet) return;
    daten[aktuellesGeraet].startsaldo = Number(startsaldo.value) || 0;
    anzeigen();
}

function addEintrag() {
    if (!aktuellesGeraet) return;
    const wert = Number(neuerEintrag.value);
    if (wert <= 0) return;
    daten[aktuellesGeraet].eintraege.push({ menge: wert });
    neuerEintrag.value = "";
    speichern();
    anzeigen();
}

function anzeigen() {
    let total = daten[aktuellesGeraet].startsaldo;
    daten[aktuellesGeraet].eintraege.forEach(e => total += e.menge);
    document.getElementById("total").innerText = total;
}

function laufwechsel() {
    if (!aktuellesGeraet) return;
    if (!confirm("Neuen Lauf starten?")) return;
    daten[aktuellesGeraet] = { startsaldo: 0, eintraege: [] };
    startsaldo.value = 0;
    anzeigen();
}

function geraetLoeschen() {
    if (!aktuellesGeraet) return;
    if (!confirm("Sportgerät wirklich löschen?")) return;
    delete daten[aktuellesGeraet];
    aktuellesGeraet = "";
    speichern();
    ladeDropdown();
    total.innerText = "0";
}

function exportCSV() {
    if (!aktuellesGeraet) return;
    let csv = "Menge\n";
    daten[aktuellesGeraet].eintraege.forEach(e => csv += e.menge + "\n");
    const blob = new Blob([csv], { type: "text/csv" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = "schussliste.csv";
    a.click();
}

window.onload = ladeDropdown;
</script>

</body>
</html>
